# Specs

The source of truth for this repo. Code is built from these.
One folder per piece of work, numbered: `specs/001-name/`.
Each folder holds `research.md`, `spec.md` and, when the change earns
it, `plan.md`.
